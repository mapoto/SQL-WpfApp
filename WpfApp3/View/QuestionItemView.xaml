<UserControl x:Class="WpfApp3.View.QuestionItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp3.View" xmlns:viewmodel="clr-namespace:WpfApp3.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:ContentViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightGray">
        <StackPanel Margin="20">
            <Label Name="QuestionIdLabel" Content="Question Item ID"/>
            <TextBox Name="QuestionIdTextBox" Text="{Binding SelectedQuestionItem.Id, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Name="DateLabel" Content="Date"/>
            <TextBlock Name="DateTextBox" Text="{Binding SelectedQuestionItem.Date, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Name="QuestionLabel" Content="Question"/>
            <TextBox Name="QuestionTextBox" Height="100" Text="{Binding SelectedQuestionItem.Question, UpdateSourceTrigger=PropertyChanged}"/>
            <StackPanel Orientation="Horizontal">
                <Label Name="OptionsLabel" Content="Options"/>
                <Button Name ="Add" Content="+" />
                <Button Name ="Delete" Content="-" />
            </StackPanel>
            <ListBox Name="OptionTextBox" ItemsSource="{Binding SelectedQuestionItem.Choices, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
            <Label Name="SolutionLabel" Content="Solution"/>
            <TextBox Name="SolutionTextBox" Text="{Binding SelectedQuestionItem.Solution, UpdateSourceTrigger=PropertyChanged}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"  Width="Auto">
                <Button Name="ModifyBtn" Content="Modify" Padding="5" IsEnabled="False" />
                <Button Name="ResetBtn" Content="Reset"  Padding="5" IsEnabled="False"/>
                <Button Name="RemoveBtn" Content="Remove" Padding="5" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
